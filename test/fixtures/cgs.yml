# == Schema Information
#
# Table name: cgs
#
#  id                  :integer          not null, primary key
#  guest_names         :string
#  contribution_amount :decimal(, )
#

cg_one:
  guest_names: 'Person One'
